<form method="post" action="{{ path('app_meteo_data_delete', {'id': meteo_datum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meteo_datum.id) }}">
    <div class="button-container">
        <button class="btn btn-danger">Delete</button>
    </div>
</form>
