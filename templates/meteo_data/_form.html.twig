{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="button-container">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>

{{ form_end(form) }}
